Run the <skill name> agent with the user's query.

Execute this command and print the output verbatim:

```bash
python ~/.claude/commands/skill_template.py "$ARGUMENTS"
```

If the command fails, display the error message as-is.
